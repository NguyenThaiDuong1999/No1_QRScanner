package com.tkhapp.qrcode.scanqr.barcodescanner.qrscanner.qrgenerator.feature.splash

import androidx.lifecycle.ViewModel

class SplashViewModelNo1 : ViewModel() {
    /*private var apiService: ApiService? = null

    init {
        apiService = RetrofitClient.createService()
    }

    fun getFixturesByDate(calendar: Calendar, apiToken: String, timeZone: String, perPage: Int, include: String, page: Int, progressBar: ProgressBar, reset: Boolean) {
        Log.d("callFixturesByDateHi", "startCall: api_token: $apiToken - timezone: $timeZone")
        progressBar.visibility = View.VISIBLE
        val sdf = SimpleDateFormat("yyyy-MM-dd", Locale.getDefault())
        val date = sdf.format(calendar.time)
        val callFixturesByDate = apiService?.getFixturesByDate(
            date,
            apiToken,
            timeZone,
            perPage,
            include,
            page
        )
        callFixturesByDate?.enqueue(object : Callback<DataFixtures> {
            override fun onResponse(call: Call<DataFixtures>, response: Response<DataFixtures>) {
                progressBar.visibility = View.GONE
                val responseData: DataFixtures? = response.body()
            }

            override fun onFailure(call: Call<DataFixtures>, t: Throwable) {
                progressBar.visibility = View.GONE
            }
        })
    }*/
}